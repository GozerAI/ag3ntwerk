apiVersion: v1
kind: Service
metadata:
  name: {{ include "ag3ntwerk.fullname" . }}
  labels:
    {{- include "ag3ntwerk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ag3ntwerk.selectorLabels" . | nindent 4 }}
